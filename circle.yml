machine:
  services:
    - docker
    - docker-compose build

## Customize dependencies
dependencies:
  override:
    - npm install -g yarn
    - yarn global add @angular/cli
    - yarn

compile:
  override:
    - ng build

    
test:
  override:
    - ng test --single-run

