machine:
  node:
    version: 0.10.22

## Customize dependencies
dependencies:
  override:
    - npm install -g yarn
    - yarn global add @angular/cli
    - yarn


    
test:
  override:
    - ng test --single-run

