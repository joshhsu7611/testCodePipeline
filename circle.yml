machine:
  services:
    - docker

## Customize dependencies
dependencies:
  override:
   - docker-compose build

compile:
  override:
    - docker-compose up

    
test:
  override:
    - docker-compose exec ooha-app ng test --single-run

