machine:
  services:
    - docker

## Customize dependencies
dependencies:
  override:
   - docker-compose build

compile:
  override:
    - docker-compose up

    
test:
  override:
    - docker-compose run ooha-app ng test --single-run

