machine:
  node:
    version: v6.1.0

## Customize dependencies
dependencies:
  override:
    - npm install -g yarn
    - yarn global add @angular/cli
    - yarn


    
test:
  override:
    - ng test --single-run

